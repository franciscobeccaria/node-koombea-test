# Node Environment
NODE_ENV="development"

# Server Configuration
PORT=3000

# Database Configuration (PostgreSQL via Docker)
# This project runs entirely in Docker with PostgreSQL
# The hostname 'postgres' refers to the PostgreSQL service defined in docker-compose.yml
DATABASE_URL="postgresql://postgres:postgres@postgres:5432/koombea_dev"

# PostgreSQL Credentials (used by docker-compose)
DB_USER="postgres"
DB_PASSWORD="postgres"
DB_NAME="koombea_dev"

# JWT Configuration
# IMPORTANT: Change these values in production!
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production"
JWT_REFRESH_SECRET="your-super-secret-refresh-key-change-this-in-production"
